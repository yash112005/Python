# python can be used to perform operation on a file
# text file : .txt,.docx,.log etc
# binary file : .mp4,.mov,.png,.jpeg
# f = open("file_name","mode")
# file_name could be sample.txt, demo.docx
# mode could be read mode,write mode
# by defalut mode is read mode
# l = f.readline()  to read a line by line 

# "r" = open for reading(default)
# "w" = open for writing,truncating the file first
# "x" = create a new file and open it for writing 
# "a" = open for writing, appending to the end of the file if it exist 
# "b" = binary mode
# "t" = text mode (default)
# "+" = open a disk file for updating (reading and writing)